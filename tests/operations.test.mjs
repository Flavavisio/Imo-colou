import {test} from 'node:test';import assert from 'node:assert/strict';
import vm from 'node:vm';import ts from 'typescript';import {readFileSync} from 'node:fs';import * as commercial from '../lib/commercial.ts';
const context=vm.createContext({Intl,Date,Math});const mod=new vm.SourceTextModule(ts.transpileModule(readFileSync('lib/operations.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022}}).outputText,{context});await mod.link(async()=>new vm.SyntheticModule(Object.keys(commercial),function(){for(const [k,v]of Object.entries(commercial))this.setExport(k,v)},{context}));await mod.evaluate();const {renewUntil,licenseCategory,todayLisbon}=mod.namespace;
test('Renewals clamp month ends and preserve paid remainder',()=>{assert.equal(renewUntil('2026-01-31',1,'2026-01-01'),'2026-02-28');assert.equal(renewUntil('2028-01-31',1,'2028-01-01'),'2028-02-29');assert.equal(renewUntil('2028-02-29',12,'2028-01-01'),'2029-02-28');assert.equal(renewUntil('2026-01-01',1,'2026-09-21'),'2026-10-21');assert.equal(renewUntil('',3,'2026-09-21'),'2026-12-21');});
test('License categories and Portugal local day',()=>{assert.equal(licenseCategory({status:'active',validUntil:'2026-09-20'},'2026-09-21'),'expired');assert.equal(licenseCategory({status:'active',validUntil:'2026-09-21'},'2026-09-21'),'soon');assert.equal(licenseCategory({status:'paused',validUntil:'2026-09-20'},'2026-09-21'),'paused');assert.equal(todayLisbon(new Date('2026-09-21T23:30:00Z')),'2026-09-22');});
